# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=whatsapp_bot_dev
DB_USER=postgres
DB_PASSWORD=password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4
OPENAI_CONTENT_FILTER=false
OPENAI_ALLOW_EXPLICIT=false

# WhatsApp Web (unofficial) Configuration
# WasenderAPI Configuration (use Personal Access Token)
WASENDER_API_BASE_URL=https://api.wasenderapi.com
WASENDER_API_TOKEN=your-personal-access-token
# Optional: If managing multiple sessions by ID, set it; otherwise token may imply session
WASENDER_SESSION_ID=
# Optional: Webhook signature verification secret. If set, incoming webhook signatures are verified.
WASENDER_WEBHOOK_SECRET=
# Optional: Webhook URL for real-time event notifications
WASENDER_WEBHOOK_URL=

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000 