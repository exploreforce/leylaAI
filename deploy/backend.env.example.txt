# Backend runtime environment
PORT=5000
NODE_ENV=production
FRONTEND_URL=https://your-domain.example.com

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_CONTENT_FILTER=true
OPENAI_ALLOW_EXPLICIT=false

# WasenderAPI credentials (Personal Access Token preferred)
WASENDER_API_BASE_URL=https://api.wasenderapi.com
WASENDER_API_TOKEN=
# If applicable: target session id for QR/status/connect
WASENDER_SESSION_ID=
# Optional: webhook signature secret used by /api/webhooks/wasender
WASENDER_WEBHOOK_SECRET=

# Database (optional; leave empty for SQLite on volume)
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_SSL=false


